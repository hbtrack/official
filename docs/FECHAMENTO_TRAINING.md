<!-- STATUS: NEEDS_REVIEW -->



## Plan: Finalização Módulo Training com Templates Customizados CRUD

Implementar backend templates org-scoped seed inicial, criar página configurações CRUD completo (criar/editar/duplicar/deletar físico/favoritar), integrar templates dinâmicos 100% banco com preview antes aplicar, corrigir UX modal criação treinos fullscreen mobile, completar Analytics dashboard, integrar Wellness APIs, finalizar navegação, automatizar E2E (Step 29).

### Steps

1. **Backend Templates com Seed Padrão (PRIORIDADE BASE)** — Criar migration `session_templates` table (id, org_id, name, description, icon [VARCHAR 20 CHECK IN ('target','activity','bar-chart','shield','zap','flame')], focus_attack_positional_pct, focus_defense_positional_pct, focus_transition_offense_pct, focus_transition_defense_pct, focus_attack_technical_pct, focus_defense_technical_pct, focus_physical_pct, **is_favorite BOOLEAN DEFAULT false**, is_active, created_by_membership_id, created_at, updated_at), **seed trigger automático** `CREATE TRIGGER trg_insert_default_templates AFTER INSERT ON organizations FOR EACH ROW BEGIN INSERT INTO session_templates (org_id, name, description, icon, focus_attack_positional_pct, focus_defense_positional_pct, focus_transition_offense_pct, focus_transition_defense_pct, focus_attack_technical_pct, focus_defense_technical_pct, focus_physical_pct, is_favorite) VALUES (NEW.id, 'Treino Tático Ofensivo', 'Foco em ataque posicional e transição ofensiva', 'target', 45, 10, 25, 5, 10, 0, 5, false), (NEW.id, 'Treino Físico', 'Alta intensidade física com técnica', 'activity', 5, 5, 5, 5, 10, 10, 60, false), (NEW.id, 'Treino Equilibrado', 'Distribuição uniforme entre todos os focos', 'bar-chart', 15, 15, 15, 15, 10, 10, 20, false), (NEW.id, 'Treino de Defesa', 'Foco em defesa posicional e transição defensiva', 'shield', 5, 50, 5, 30, 0, 5, 5, false); END;` trigger garante 4 templates padrão APENAS para novas orgs criadas após migration (orgs existentes NÃO receberão), implementar router session_templates.py: **GET** `/session-templates` (org-scoped, ORDER BY is_favorite DESC, name ASC, retorna WHERE deleted_at IS NULL, limite 50 total ativos), **POST** `/session-templates` (validar COUNT < 50 incluindo seed, constraint soma focos ≤ 120%), **PATCH** `/session-templates/:id` (org-scoped ownership, templates seed editáveis SEM restrição, permitir editar mesmo se usado em sessões, aceitar campo `is_favorite`), **PATCH** `/session-templates/:id/favorite` (toggle favorito rápido endpoint dedicado `UPDATE session_templates SET is_favorite = NOT is_favorite WHERE id = :id AND org_id = :org_id`), **DELETE** `/session-templates/:id` (hard delete físico `DELETE FROM session_templates WHERE id = :id AND org_id = :org_id`, libera espaço pro limite 50), atualizar SCHEMA_CANONICO_DATABASE.md documentar trigger seed automático para novas orgs + campo is_favorite

2. **Cleanup duplicados** — Deletar folders avaliacoes/training/avaliacoes), presencas/training/presencas), remover ProfessionalSidebar.tsx linhas 84-85

3. **Fix navegação 8 tabs** — Atualizar TrainingTabs.tsx para 8 tabs: AGENDA | CALENDARIO | PLANEJAMENTO | EXERCICIOS | ANALYTICS | RANKINGS | EFICACIA | CONFIGURACOES, corrigir `/banco` → `/exercise-bank`, adicionar Rankings entre Analytics e Eficacia

4. **Remover templates hardcoded frontend** — Deletar exportações `FOCUS_TEMPLATES`, `FocusTemplate`, `getFocusTemplate()` de trainings.ts linhas 577-723, manter apenas interfaces `FocusValues` e funções validação `getFocusStatus()`, `validateJustification()`, `calculateFocusTotal()`, criar API client `getSessionTemplates()`, `createSessionTemplate(payload)`, `updateSessionTemplate(id, payload)`, `toggleTemplateFavorite(id)` PATCH `/session-templates/:id/favorite`, `deleteSessionTemplate(id)` hard delete, `duplicateSessionTemplate(id, newName)` POST `{...template, name: newName, id: undefined, is_favorite: false}`

5. **Frontend Configurações Templates CRUD (PRIORIDADE 1)** — Criar /training/configuracoes/page.tsx/training/configuracoes/page.tsx) + `ConfiguracoesClient.tsx` (520L) com aba única **Templates de Treino**: Header contador "X/50 templates" + botão "Criar Template" (disabled se ≥50 + tooltip "Limite atingido"), Table colunas (⭐ Favorito star icon toggle onClick, Ícone emoji 28px, Nome bold text-base, Descrição text-sm text-gray-600 truncate max-w-xs, Focos Principais [3 badges maiores % ordem decrescente coloridos bg-blue/green/orange], Actions dropdown menu [Edit/Duplicate/Delete]), **favoritos exibidos primeiro** com divider visual após favoritos, **CreateTemplateModal** (480L) max-w-3xl: grid 2 cols (esquerda form: input nome required max 50 chars, textarea descrição max 200 chars rows 3, radio grid ícones 3x2 emojis grandes 40px, checkbox "⭐ Marcar como favorito", 7 sliders focos % range 0-100 com label left + valor right | direita preview: validação semáforo badge topo grande, pizza chart Recharts 280x280, soma total badge XL), botão "Salvar Template" POST `/session-templates` disabled se vermelho, **EditTemplateModal** (mesma estrutura + pre-fill + PATCH, checkbox favorito editável, SEM restrição se template usado), **Duplicate** action `const handleDuplicate = async (template) => { const newName = prompt('Nome da cópia:', \`Cópia de ${template.name}\`); if(!newName) return; await duplicateSessionTemplate(template.id, newName); toast.success('Template duplicado'); refetch(); }`, **Favorite** toggle `const handleToggleFavorite = async (template) => { await toggleTemplateFavorite(template.id); refetch(); }` star icon muda cor gold/gray, **Delete** confirm dialog red "Deletar template permanentemente? Esta ação NÃO pode ser desfeita. Sessões passadas não serão afetadas" DELETE físico `/session-templates/:id`

6. **Integrar templates 100% banco com Preview** — Atualizar FocusTemplates.tsx remover import `FOCUS_TEMPLATES` hardcoded, buscar 100% banco: `const { data: templates, isLoading } = useQuery({ queryKey: ['session-templates'], queryFn: getSessionTemplates, staleTime: 1000 * 60 * 5 })`, **ordenar favoritos primeiro** `const sortedTemplates = useMemo(() => templates?.sort((a, b) => b.is_favorite - a.is_favorite || a.name.localeCompare(b.name)) || [], [templates])`, renderizar `{isLoading ? <div className="grid grid-cols-4 gap-3">{[1,2,3,4].map(i => <Skeleton key={i} className="h-40" />)}</div> : sortedTemplates.map(template => <button key={template.id} onClick={() => handlePreview(template)} ... />{template.is_favorite && <Star className="absolute top-2 left-2 w-4 h-4 fill-yellow-400 text-yellow-400" />}</button>)}`, **Preview Modal antes aplicar**: criar `TemplatePreviewModal` (220L) Dialog max-w-2xl `<Dialog open={showPreview}><DialogContent><DialogHeader><DialogTitle className="flex items-center gap-2">{template.icon} {template.name}</DialogTitle><p className="text-sm text-gray-600">{template.description}</p></DialogHeader><div className="space-y-4"><h4 className="font-semibold">Distribuição de Focos:</h4><div className="grid grid-cols-2 gap-4"><div className="space-y-2">{FOCUS_FIELDS.map(f => <div key={f.key} className="flex justify-between"><span>{f.label}</span><span className="font-semibold">{template.focus[f.key]}%</span></div>)}</div><FocusDistributionPieChart focus={template.focus} size="md" showLegend /></div></div><DialogFooter><Button variant="outline" onClick={() => setShowPreview(false)}>Cancelar</Button><Button onClick={() => { onSelectTemplate(template.focus); setShowPreview(false); toast.success('Template aplicado'); }}>Aplicar Template</Button></DialogFooter></DialogContent></Dialog>`, handler `const handlePreview = (template) => { setPreviewTemplate(template); setShowPreview(true); }`, grid responsivo `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3`, adicionar link footer `<Link href="/training/configuracoes" className="flex items-center gap-2 text-sm text-blue-600 hover:underline"><Settings className="w-4 h-4" /> Gerenciar Templates</Link>`

7. **CORRIGIR UX Modal Criação Treinos Fullscreen Mobile (CRÍTICO)** — Atualizar CreateSessionModal.tsx corrigir problemas visíveis na imagem: **1) Tamanho responsivo** alterar linha 230 container `className="relative w-full bg-white dark:bg-[#0f0f0f] shadow-2xl border max-w-xl lg:max-w-5xl max-h-[90vh] lg:max-h-[95vh] rounded-none sm:rounded-xl overflow-hidden flex flex-col"` desktop max-w-5xl + **mobile fullscreen** `fixed inset-0 lg:relative lg:inset-auto` sem rounded mobile, **2) Layout 2 colunas desktop** reestruturar form section linhas 240-420: `<div className="grid grid-cols-1 lg:grid-cols-2 gap-6 p-4 sm:p-6">` envolver conteúdo, **coluna esquerda** (data/hora/tipo/objetivo/duração/templates) `<div className="space-y-5 lg:col-span-1">`, **coluna direita** (focos sliders + pizza) `<div className="space-y-5 lg:col-span-1 lg:sticky lg:top-0">`, **3) Templates cards** alterar grid linha 320 `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-2 sm:gap-3` melhor espaçamento mobile, aumentar padding cards `p-3 sm:p-4`, texto label `text-xs sm:text-sm`, emojis `text-lg sm:text-xl`, **4) Sliders focos** aumentar espaçamento `space-y-4`, label width `w-28 sm:w-36`, font `text-sm sm:text-base`, slider height `h-2 sm:h-3` mais visível, valor display `text-sm sm:text-base font-semibold`, **5) Pizza chart** aumentar size condicional `size={isMobile ? "sm" : "md"}` (200px mobile / 300px desktop), sempre visível sem scroll, **6) Badge validação** posição adaptativa `absolute top-0 right-0 lg:sticky lg:top-0`, size `text-sm sm:text-base px-3 sm:px-4 py-1.5 sm:py-2`, **7) Mobile layout** coluna única com pizza abaixo sliders em mobile, templates grid 2 cols mobile, botão close X maior mobile `w-10 h-10 sm:w-8 sm:h-8`, **8) Spacing** padding geral `p-4 sm:p-6`, gaps `gap-4 sm:gap-6`, footer padding `p-4 sm:p-6`, **9) Scrollable** conteúdo `overflow-y-auto flex-1` apenas body não header/footer

8. **Analytics dashboard (PRIORIDADE 2)** — Criar app/(admin)/analytics/page.tsx/analytics/page.tsx) + `AnalyticsDashboardClient.tsx` (350L) reutilizando WeeklyLoadChart, DeviationAlerts, WellnessResponseChart, layout: Header (título "Analytics da Equipe" + descrição + botão "Exportar PDF"), Grid 2 cols desktop/1 mobile com 8 sections (TeamSummaryCards 4 cards grid 2x2, WeeklyLoadChart full-width, DeviationAlerts + WellnessResponseChart grid 1x2, TopPerformersCard, AttendanceBreakdownPie), **botão "Exportar PDF"** onClick abre `ExportPDFModal` existente passando `teamId` + `dateRange`, React Query cache 5min, loading skeletons cada section, empty state sem equipe

9. **Calendario modal criação** — Importar `CreateSessionModal` corrigido em CalendarioClient.tsx/training/calendario/CalendarioClient.tsx), adicionar estados `const [showCreateModal, setShowCreateModal] = useState(false); const [selectedDate, setSelectedDate] = useState<string | null>(null);`, handler `const handleDayClick = (date: Date) => { const hasEvents = sessions.some(s => isSameDay(parseISO(s.session_at), date)); if (!hasEvents) { setSelectedDate(format(date, 'yyyy-MM-dd')); setShowCreateModal(true); }}`, renderizar modal após linha 270, botão flutuante "+" fixo `<button className="fixed bottom-8 right-8 w-14 h-14 bg-emerald-600 hover:bg-emerald-700 text-white rounded-full shadow-xl z-40 text-2xl transition-transform hover:scale-110">+</button>`, modal mostrará templates do banco com preview após Step 6

10. **Planejamento wizard draft** — Criar `CreateCycleWizard.tsx` (620L) Dialog fullscreen mobile/max-w-4xl desktop, multi-step 4 etapas progressbar horizontal: **Step1** radio Tipo cards visuais grandes (Macrociclo 📅 "6-12 meses" / Mesociclo 📆 "4-6 semanas") border-2 quando selected, **Step2** input Nome required + textarea Objetivo required contador chars bottom-right, **Step3** date-pickers Início/Término validação `endDate > startDate` + badge duração "X dias" calculado automaticamente, **Step4** select Parent opcional (se Meso fetch macrociclos `GET /cycles?type=macro&status=active`) + preview tree hierarquia visual indent, navegação stepper horizontal `<div className="flex items-center justify-center gap-2 mb-6">{steps.map((s, i) => <div key={i} className={cn("h-2 rounded-full transition-all", i <= currentStep ? "w-12 bg-emerald-500" : "w-8 bg-gray-300")} />)}</div>` + buttons footer `<div className="flex justify-between"><Button variant="outline" onClick={handleBack} disabled={currentStep === 0}>Voltar</Button><Button onClick={handleNext}>{currentStep === 3 ? 'Criar Ciclo' : 'Próximo'}</Button></div>`, **localStorage draft**: `const DRAFT_KEY = 'cycle-draft'; const saveDraft = useDebouncedCallback((data) => { localStorage.setItem(DRAFT_KEY, JSON.stringify(data)); }, 500); useEffect(() => { saveDraft(formData); }, [formData]); useEffect(() => { const draft = localStorage.getItem(DRAFT_KEY); if (draft) { const parsed = JSON.parse(draft); setFormData(parsed); toast.info('📝 Rascunho restaurado', { description: 'Continue de onde parou' }); }}, []);`, **button descartar** header secundário `<button type="button" onClick={() => { if(confirm('Descartar rascunho?')) { localStorage.removeItem(DRAFT_KEY); setFormData(initialFormData); toast.success('Rascunho descartado'); }}} className="text-sm text-red-600 hover:underline flex items-center gap-1"><Trash2 className="w-3 h-3" /> Descartar rascunho</button>`, limpar após POST success `localStorage.removeItem(DRAFT_KEY); onSuccess();`, substituir TODO PlanejamentoClient.tsx#L48/training/planejamento/PlanejamentoClient.tsx#L48)

11. **Agenda handlers completos** — Implementar handlers reais em AgendaClient.tsx#L83-95/training/agenda/AgendaClient.tsx#L83-L95): criar `EditSessionModal.tsx` (clone CreateSessionModal corrigido file completo + adicionar prop interface `session?: TrainingSession`, pre-fill `useEffect(() => { if(session) { setFormData({ date: format(parseISO(session.session_at), 'yyyy-MM-dd'), time: format(parseISO(session.session_at), 'HH:mm'), session_type: session.session_type, main_objective: session.main_objective || '', duration_planned_minutes: session.duration_planned_minutes || 90, focuses: { focus_attack_positional_pct: session.focus_attack_positional_pct || 0, focus_defense_positional_pct: session.focus_defense_positional_pct || 0, focus_transition_offense_pct: session.focus_transition_offense_pct || 0, focus_transition_defense_pct: session.focus_transition_defense_pct || 0, focus_attack_technical_pct: session.focus_attack_technical_pct || 0, focus_defense_technical_pct: session.focus_defense_technical_pct || 0, focus_physical_pct: session.focus_physical_pct || 0 } }); }}, [session]);`, submit alterar para PATCH `/sessions/:id`), **onEdit** handler substituir console.log `const handleEdit = (session: TrainingSession) => { setEditingSession(session); setShowEditModal(true); }`, **onDuplicate** handler `const handleDuplicate = async (session: TrainingSession) => { const confirmed = await confirm({ title: 'Duplicar treino?', description: 'Criar cópia para 7 dias depois?', confirmText: 'Duplicar' }); if (!confirmed) return; const newDate = addDays(parseISO(session.session_at), 7); const payload: SessionCreate = { organization_id: session.organization_id, team_id: session.team_id, session_at: newDate.toISOString(), session_type: session.session_type, main_objective: session.main_objective, duration_planned_minutes: session.duration_planned_minutes, focus_attack_positional_pct: session.focus_attack_positional_pct, focus_defense_positional_pct: session.focus_defense_positional_pct, focus_transition_offense_pct: session.focus_transition_offense_pct, focus_transition_defense_pct: session.focus_transition_defense_pct, focus_attack_technical_pct: session.focus_attack_technical_pct, focus_defense_technical_pct: session.focus_defense_technical_pct, focus_physical_pct: session.focus_physical_pct }; await createSession(payload); toast.success('Treino duplicado com sucesso'); refetch(); }`, **onDelete** handler `const handleDelete = async (session: TrainingSession) => { const confirmed = await confirm({ title: 'Deletar treino?', description: 'Esta ação não pode ser desfeita. Sessões existentes não serão afetadas.', confirmText: 'Deletar', variant: 'destructive' }); if (!confirmed) return; await patchSession(session.id, { deleted_at: new Date().toISOString(), deleted_reason: 'user_action' }); toast.success('Treino deletado'); refetch(); }`, renderizar modais `{showEditModal && <EditSessionModal isOpen={showEditModal} onClose={() => setShowEditModal(false)} onSuccess={() => { setShowEditModal(false); refetch(); }} session={editingSession} teamId={selectedTeam?.id} />}`

12. ✅ **Wellness API integration (PRIORIDADE 3) — COMPLETO 2026-01-18** — Hook `useSessionDetail` já existia em `src/lib/hooks/useSessions.ts`, atualizado WellnessPreClient.tsx + WellnessPostClient.tsx: substituído useState + useEffect MOCK (35 linhas) por `const { session, isLoading, error } = useSessionDetail(sessionId)` (1 linha), adicionado loading state com Skeleton (h-10, h-6, h-20, h-40), adicionado error state inline com AlertCircle, imports: +useSessionDetail +Skeleton +format +ptBR -useEffect -Loader2, build verificado: npm run build Exit Code 0 TypeScript Errors 0 63 routes compiladas, próximos passos: testar wellness forms browser + validar trigger tr_calculate_internal_load POST wellness-post calcula internal_load (RPE × duration) campo readonly

13. ✅ **Sidebar visual separators — COMPLETO 2026-01-18** — Adicionado item Configurações ao array treinosSubmenu (Settings icon, href /training/configuracoes, tooltip 'Templates e configurações do módulo'), substituído componente SidebarSubmenu genérico por renderização customizada com 3 seções: PLANEJAMENTO (Agenda/Planejamento/Exercícios slice 0-3), separator border-t my-2, ANÁLISE (Analytics/Eficácia slice 3-5), separator, CONFIGURAÇÃO (Configurações slice 5+), headings text-xs font-semibold uppercase tracking-wider gray-500/400, padding heading1 px-3 pt-4 pb-2, headings2-3 px-3 pt-2 pb-2, links active bg-emerald-50/emerald-900/20, hover bg-gray-100/800, fallback SidebarSubmenu quando isCollapsed=true, build verificado: Exit Code 0 TypeScript Errors 0 63 routes compiladas

14. **🔄 Playwright E2E Training (Step 29) — EM PROGRESSO** — ✅ **Seed OPÇÃO B Completo Implementado (2026-01-18 18:04)**: Script `seed_e2e.py` atualizado com 4 novas funções para suportar testes E2E Training Module, criando dados completos: (1) `seed_e2e_templates()` - 4 templates padrão (Tático Ofensivo, Físico, Equilibrado, Defesa) na tabela `session_templates` com org_id correto, (2) `seed_e2e_training_cycles()` - 2 macrociclos (Preparatório ativo, Competitivo ativo) + 2 mesociclos (Fase 1/2) na tabela `training_cycles`, (3) `seed_e2e_training_sessions_dirigente()` - 6 sessões de treino (3 passadas status=closed, 3 futuras status=draft) na tabela `training_sessions` para equipe do dirigente, (4) `seed_e2e_wellness_data()` - 6 registros wellness (3 pré + 3 pós) nas tabelas `wellness_pre` e `wellness_post` usando atleta dinâmico da equipe coordenador. **Correções de Schema Aplicadas**: (a) `session_templates.organization_id` → `org_id`, (b) `training_cycles.cycle_type` → `type` + `name` removido, (c) `training_cycles.status` valores permitidos `active/completed/cancelled` (não `planned`), (d) `training_sessions.status` valores `draft/in_progress/closed/readonly` (não `planned`), (e) `training_sessions.template_id` removido (coluna não existe), (f) `wellness_pre.sleep_quality` numérico 1-5 (não texto), (g) `wellness_post.perceived_intensity` numérico 1-5 (não `alta/media/baixa`), (h) `wellness_*.athlete_id` usa `athletes.id` (não `person_id`). **Seed executado com sucesso**: 0 erros, mensagem final exibe dados criados para testes E2E Training Module. ⏳ **Próximo**: Executar `/tests/e2e/training-module.spec.ts` (850L) com 20 test cases automatizados usando fixtures equipe/organização: **1)** `test('deve navegar 8 tabs Training', async ({ page }) => { await page.goto('/training/agenda'); const tabs = ['agenda', 'calendario', 'planejamento', 'exercise-bank', 'analytics', 'rankings', 'eficacia-preventiva', 'configuracoes']; for (const tab of tabs) { await page.click(\`[href*="${tab}"]\`); const response = await page.waitForResponse(res => res.url().includes('session') || res.url().includes('cycle')); expect(response.status()).toBeLessThanOrEqual(200); }})`, **2-6)** CRUD templates `test('deve criar template customizado', async ({ page }) => { await page.goto('/training/configuracoes'); await page.click('button:has-text("Criar Template")'); await page.fill('input[name="name"]', 'Treino Goleiro'); await page.fill('textarea[name="description"]', 'Treinamento específico para goleiros'); await page.click('input[value="shield"]'); await page.check('input[name="is_favorite"]'); await page.fill('input[name="focus_defense_technical_pct"]', '80'); await page.fill('input[name="focus_defense_positional_pct"]', '10'); await page.fill('input[name="focus_physical_pct"]', '10'); await expect(page.locator('.semaforo-badge:has-text("100%")')).toHaveClass(/verde/); await page.click('button:has-text("Salvar Template")'); await expect(page.locator('text=Template criado')).toBeVisible(); const row = page.locator('table tr:has-text("Treino Goleiro")'); await expect(row.locator('.star-icon')).toHaveClass(/fill-yellow/); }); test('deve editar template', ...); test('deve duplicar template', async ({ page }) => { const duplicateBtn = page.locator('button[aria-label="Duplicar"]:near(:text("Treino Goleiro"))'); await duplicateBtn.click(); await page.fill('input[type="text"]', 'Treino Goleiros Avançado'); await page.click('button:has-text("Confirmar")'); await expect(page.locator('text=Treino Goleiros Avançado')).toBeVisible(); }); test('deve favoritar template', async ({ page }) => { const star = page.locator('.star-icon:near(:text("Treino Físico"))'); await star.click(); await expect(star).toHaveClass(/fill-yellow/); const firstRow = page.locator('table tbody tr').first(); await expect(firstRow).toContainText('Treino Físico'); }); test('deve deletar template', ...);`, **7)** limite 50 templates `test('deve bloquear criação quando 50 templates', async ({ page }) => { for(let i=0; i<46; i++) { await createTemplateViaAPI(\`Template ${i}\`); } await page.goto('/training/configuracoes'); const createBtn = page.locator('button:has-text("Criar Template")'); await expect(createBtn).toBeDisabled(); await createBtn.hover(); await expect(page.locator('text=Limite de 50 templates atingido')).toBeVisible(); });`, **8)** preview template `test('deve mostrar preview antes aplicar template', async ({ page }) => { await page.goto('/training/agenda'); await page.click('button:has-text("Criar Sessão")'); await page.click('.template-card:has-text("Treino Físico")'); await expect(page.locator('dialog:has-text("Distribuição de Focos")')).toBeVisible(); await expect(page.locator('text=Físico: 60%')).toBeVisible(); await page.click('button:has-text("Aplicar Template")'); await expect(page.locator('input[name="focus_physical_pct"]')).toHaveValue('60'); });`, **9)** modal UX mobile `test('deve exibir modal fullscreen em mobile', async ({ page }) => { await page.setViewportSize({ width: 375, height: 667 }); await page.goto('/training/agenda'); await page.click('button:has-text("Criar Sessão")'); const modal = page.locator('[role="dialog"]'); await expect(modal).toHaveClass(/fixed inset-0/); await expect(modal).toHaveClass(/rounded-none/); const templateGrid = page.locator('.template-grid'); await expect(templateGrid).toHaveClass(/grid-cols-2/); });`, **10-14)** Agenda/Calendário/Planejamento/Analytics/Rankings, **15)** draft localStorage `test('deve persistir draft planejamento', async ({ page }) => { await page.goto('/training/planejamento'); await page.click('button:has-text("Criar Ciclo")'); await page.fill('input[name="name"]', 'Test Cycle'); await page.click('button:has-text("Próximo")'); await page.waitForTimeout(600); const draft = await page.evaluate(() => localStorage.getItem('cycle-draft')); expect(draft).toBeTruthy(); const parsed = JSON.parse(draft); expect(parsed.name).toBe('Test Cycle'); await page.reload(); await expect(page.locator('text=Rascunho restaurado')).toBeVisible(); });`, **16-20)** Wellness API, TypeScript build, responsive mobile, dark mode, edge cases validação

15. **Documentação validação** — Criar docs/3-CHECKLIST_VALIDACAO_TRAINING_FINAL.md (200L) markdown estruturado: frontmatter `---\ntitle: Checklist Validação Final - Módulo Training\ndate: 2026-01-18\nstatus: Em Progresso\ntarget: 100% Pass\n---`, **18 sections detalhadas**: 1) Navegação 8 tabs (sub-items: click cada tab, verificar network 200 OK, print screenshot), 2) Sidebar 3 separators visuais (print headings "PLANEJAMENTO" "ANÁLISE" "CONFIGURAÇÃO"), 3) Templates CRUD (criar "Template Teste" → print form → salvar → print table → editar → duplicar → print "Cópia de..." → deletar confirm → print), 4) Favoritar templates (marcar favorito → verificar star gold → confirmar ordem lista favoritos primeiro → print), 5) Preview template (click template → print modal preview com distribuição focos + pizza → aplicar → confirmar sliders preenchidos), 6) Limite 50 templates (criar 50 via script → print botão disabled + tooltip "Limite atingido"), 7) Seed 4 templates org nova (criar org test → verificar 4 templates iniciais → print table), 8) Template aplicado focos (print before sliders 0% → click template → print after sliders preenchidos valores corretos), 9) Validação semáforo (testar verde ≤100% → amarelo 101-120% + justificativa obrigatória → vermelho >120% botão disabled → print cada estado), **10) MODAL UX CORRIGIDO FULLSCREEN MOBILE** (desktop: print modal width >1200px, 2 colunas visíveis, templates grid 4 cols legível, sliders espaçados, pizza chart 300px completo, sem textos sobrepostos | mobile: print fullscreen inset-0, templates grid 2 cols, pizza abaixo sliders, botão X maior, sem scroll horizontal), 11-18) Agenda/Calendário/Planejamento/Analytics/Rankings/Eficacia/Wellness/TypeScript, **Tabela resumo final**: `| # | Item | Status | Evidência | Observações |\n|---|------|--------|-----------|-------------|\n| 1 | Navegação 8 tabs | [ ] | screenshot-tabs.png | |\n| 2 | Sidebar separators | [ ] | screenshot-sidebar.png | |\n...` 18 linhas com checkboxes ✅/❌, **Execução**: validação manual checklist passo a passo + E2E paralelo `npm run test:e2e -- training-module.spec.ts`, capturar screenshots cada item pasta `docs/screenshots-validacao/training/`, gerar **relatório final** `## Resumo Executivo\n- Total items: 18\n- Passed: X (Y%)\n- Failed: Z\n- Blocker issues: lista\n- Target: 100% Pass\n\n**Assinado:** [Nome] [Data]`, anexar relatório E2E HTML report

---

**🎯 PLANO FINALIZADO E COMPLETO**

**Total:** 15 steps detalhados
**Tempo estimado:** ~14 horas implementação
**Prioridades:** Backend Base → UX Modal → Configurações CRUD → Analytics → Integrações → E2E
**Validação:** 18 sections checklist + 20 test cases E2E automatizados
**Target:** 100% completude módulo Training pronto para produção