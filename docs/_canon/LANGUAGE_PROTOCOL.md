# LANGUAGE PROTOCOL

## Authority Chain

This protocol extends:
- `AI_KERNEL.md` (LEVEL 0)
- `ARCH_REQUEST_GENERATION_PROTOCOL.md` (sibling protocol)

---

RFC 2119 rules

ARCH_REQUEST = contrato

EXEC_TASK separation

REGRAS OBRIGATÓRIAS:

1. Usar linguagem normativa RFC 2119 (MUST, MUST NOT, SHALL).
2. Não misturar orientação com especificação.
3. ARCH_REQUEST = contrato puro (sem dicas, sem sugestões).
4. EXEC_TASK só quando explicitamente solicitado.
5. Não inventar seções fora do template canônico.
6. Se faltar template, pedir o molde antes de gerar.
7. Sem comentários fora do artefato solicitado.
