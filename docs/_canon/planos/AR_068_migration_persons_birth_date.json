{
  "project": "HB Track",
  "version": "1.2.0",
  "notes": "AR_068 foi implementada em 2026-02-20 (migration 0053 aplicada, model Person atualizado). Este plano gera evidencia canonica via hb report para que o Testador possa verificar. A validation_command e file-based (deterministica 100%): verifica migration file + model + triggers no codigo. Nao requer conexao com banco para triple-run.",
  "assumptions": [
    "Migration 0053_persons_birth_date_not_null_parity.py ja foi aplicada ao banco (alembic head = 0053)",
    "Model person.py ja tem birth_date: Mapped[date] = mapped_column(sa.Date(), nullable=False)",
    "Executor deve rodar hb report para gerar evidencia canonica, NAO re-executar a migration",
    "Se banco nao estiver disponivel: validation_command ainda passa (e file-based)"
  ],
  "tasks": [
    {
      "id": "068",
      "title": "Gerar evidencia canonica AR_068 persons birth_date NOT NULL",
      "description": "AR_068 foi implementada: migration 0053 aplicada ao banco, model Person atualizado para nullable=False. Este task gera evidencia canonica via hb report. OPERACAO: rodar validation_command que verifica estado atual dos artefatos de forma deterministica (file-based). NAO re-executar a migration. Verificar: (1) arquivo de migracao existe contendo triggers e NOT NULL; (2) model person.py tem nullable=False; (3) arquivo de migracao contendo fn_sync e fn_validate.",
      "criteria": "- migration file 0053_persons_birth_date_not_null_parity.py existe em alembic/versions/\n- migration contem fn_sync_birth_date_athletes_to_persons\n- migration contem fn_validate_birth_date_parity_on_person\n- migration contem trg_validate_birth_date_persons\n- migration contem nullable=False (ALTER COLUMN)\n- model person.py tem birth_date com nullable=False\n- hb report gera evidence exit 0",
      "validation_command": "python -c \"import pathlib; v = pathlib.Path('Hb Track - Backend/db/alembic/versions'); migs = [f for f in v.iterdir() if '0053' in f.name and f.suffix == '.py']; assert migs, f'FAIL: migration 0053 nao encontrada em {list(v.iterdir())}'; src = migs[0].read_text(encoding='utf-8'); assert 'fn_sync_birth_date_athletes_to_persons' in src, 'FAIL: sync trigger function ausente'; assert 'fn_validate_birth_date_parity_on_person' in src, 'FAIL: parity trigger function ausente'; assert 'trg_validate_birth_date_persons' in src, 'FAIL: validate trigger ausente'; assert 'nullable=False' in src, 'FAIL: NOT NULL constraint ausente na migration'; m = pathlib.Path('Hb Track - Backend/app/models/person.py').read_text(encoding='utf-8'); assert 'nullable=False' in m and 'birth_date' in m, 'FAIL: person.py nao tem birth_date nullable=False'; print('PASS AR_068: persons.birth_date NOT NULL + triggers verificados')\"",
      "write_scope": [
        "Hb Track - Backend/db/alembic/versions/0053_persons_birth_date_not_null_parity.py",
        "Hb Track - Backend/app/models/person.py"
      ],
      "rollback_plan": "git checkout -- \"Hb Track - Backend/app/models/person.py\"",
      "notes": "Migration ja aplicada. Rollback de DB (se necessario): alembic downgrade -1 — mas NAO executar sem intervencao humana (remove triggers e reverte nullable). Este plano apenas valida estado existente para selo."
    }
  ]
}
