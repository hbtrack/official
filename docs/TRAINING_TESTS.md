<!-- STATUS: NEEDS_REVIEW -->

Plan: Testes E2E Training Modularizados - 100% Coverage
Criar suite completa E2E modularizada para Training elevando cobertura de 69% (18/26) para 100% (50+ testes) usando Opção A (inputs visíveis), UUIDs determinísticos, aproveitando Steps 6-9 implementados (1,897L), migração incremental, e helpers híbridos (núcleo compartilhado, lógica local).

Steps
Verificar + Criar Estrutura Base (2h) - ✅ Confirmar Steps 6-9 implementados (wellness_notification_service.py 440L, wellness_gamification_service.py 749L, team_wellness_ranking_service.py 708L), analisar training-module.spec.ts atual (739L, 20 testes, 18 passing), criar estrutura tests/e2e/training/helpers/ com 3 helpers núcleo: auth.helpers.ts (setupMultiRole, login por role), seed.helpers.ts (lookupUuidByName, getFixtureIds), assertion.helpers.ts (expect wrappers genéricos sem regra de negócio), executar baseline atual garantindo 18/26 green antes de iniciar migração

Criar Seed Canônico + Mapeamento (3h) - Implementar deterministic_uuid(namespace, name) usando uuid.uuid5(NAMESPACE_DNS, f"{namespace}:{name}") em seed_e2e_canonical.py, gerar 32 users (email hash), 16 teams (slug categoria-genero), 240 athletes (nome completo), 320 sessions (team-date-type), 50 notifications, badges/rankings determinísticos, criar tabela mapeamento em SEED_CANONICO.md: Entidade | Namespace | Name | UUID | Usado em Testes | Contagem Esperada | Asserções Específicas (ex: "ATHLETE_JOAO_ID | athletes | joao-silva | [uuid] | training-wellness-athlete.spec.ts (TC-21, TC-22), training-gamification.spec.ts (TC-31) | 1 athlete, 10 sessions, 2 badges | expect(response.athlete_id).toBe(ATHLETE_JOAO_ID)"), atualizar training-CONTRACT.md seção "Cenários de Teste" com 50 TC organizados por arquivo spec

Corrigir Bloqueadores + Migrar Arquivo 1 (7h) - Aplicar Opção A em CreateTemplateModal.tsx/EditTemplateModal.tsx: substituir type="hidden" por type="number" className="sr-only" com onChange bidirecional, adicionar timing fixes (waitForLoadState, waitForSelector com data-testid), implementar APIs attendance/wellness_pre/wellness_post com idempotência via ON CONFLICT DO UPDATE, ajustar thresholds p95 <300ms local, <450ms CI, executar testes atualizados validando fixes, migrar incrementalmente arquivo 1: criar training-navigation.spec.ts (3 tests movidos de A1 + navigation extras), usar apenas helpers núcleo auth.helpers.ts, validar 3/3 green antes de continuar

Migrar Arquivos 2-7 + Criar 6 Novos (12h) - Migrar incrementalmente (1 por vez, validar green): training-templates.spec.ts (8 tests B1-B5, C1-C2, G5), training-sessions.spec.ts (✅ PASS 2026-01-XX: TC-E1/TC-E2 8/8), training-planning.spec.ts (✅ PASS 2026-01-XX: TC-E3/TC-F1 2/2), training-analytics.spec.ts (3 tests E4-E5), training-mobile-ux.spec.ts (3 tests D1, G3), training-validation.spec.ts (5 tests G1-G2-G4 + edge cases), criar 6 novos arquivos com lógica local: training-wellness-athlete.spec.ts (6 tests TC-21 a TC-26, helpers locais: submitWellnessPre/Post com presets hardcoded), training-wellness-coach.spec.ts (2 tests TC-27/28, helpers locais: getWellnessStatus com grid parsing), training-gamification.spec.ts (5 tests TC-31 a TC-35, helpers locais: verifyBadge com cálculo response_rate, getTeamRanking com sort logic), training-lgpd.spec.ts (3 tests export/anonimização), training-performance.spec.ts (5 tests queries/cache/bundle), training-accessibility.spec.ts (4 tests aria/keyboard/focus/tours) - modelo híbrido: regras de negócio (wellness thresholds, badge formulas, ranking calculation) ficam em cada spec para autonomia e debugging claro

Pipeline + Documentação Final (3h) - Criar training-TEST-CONTRACT.md com 50 cenários Given/When/Then organizados por arquivo spec incluindo precondições seed (UUIDs conhecidos e mapeados em tabela), executar pipeline completo: reset-and-start.ps1 → seed_e2e_canonical.py --deterministic → npx playwright test tests/e2e/training --project=chromium com paralelização híbrida: fullyParallel: true para arquivos base (navigation, templates, sessions, planning, analytics, mobile, validation - 7 arquivos ~70% tempo reduzido), fullyParallel: false em describe blocks seed-dependent (wellness, gamification, lgpd - 6 arquivos serial evitando DB locks), gerar HTML report validando métricas (queries p95 <300ms, cache hit >80%, WCAG AA), atualizar FECHAMENTO_LOG.md Step 14 "✅ 100% (50/50 passed)" breakdown por arquivo, marcar FECHAMENTO_TRAINING.md Step 15 checklist, atualizar 1- ACCESSIBILITY_CHECKLIST.md

Further Considerations
Migração Incremental Confirmada - Executar baseline atual (18/26 green) primeiro, migrar 1 arquivo por vez validando sem regressão após cada migração, commit incremental para rastreabilidade completa, rollback simples se falha isolada - estratégia segura para primeira modularização eliminando risco big-bang

Modelo Híbrido de Helpers - DECISÃO FINAL - 🔒 Helpers núcleo compartilhados (auth.helpers.ts, seed.helpers.ts, assertion.helpers.ts) sem regra de negócio, apenas utilitários genéricos; 🔓 Lógica local em cada spec (wellness presets, gamification formulas, ranking calculations) - trade-off: duplicação leve (~20-30 linhas por arquivo) é aceitável quando reduz acoplamento cognitivo e acelera debugging, evitando mini-framework oculto que dificulta manutenção

Paralelização Configurável por Describe - Playwright suporta describe.configure({ mode: 'serial' }) em blocks específicos mantendo fullyParallel: true global - aplicar serial apenas em describes que manipulam seed ou dependem de estado compartilhado (wellness submissions, badge calculations), permitindo execução paralela de read-only tests (navigation, analytics, accessibility) maximizando velocidade sem aumentar flakiness
