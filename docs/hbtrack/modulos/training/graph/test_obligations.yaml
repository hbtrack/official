version: "1.0.0"
status: active
module: training

obligations:
  - id: TRAIN-TO-001
    scope: sovereign_schema
    artifact_ref: "../../../../../contracts/schemas/training/training_session.schema.json"
    verification: "JSON Schema validation via contract gates"
    evidence_refs:
      - "../../../../../_reports/contract_gates/latest.json"

  - id: TRAIN-TO-002
    scope: openapi_contract
    artifact_ref: "../../../../../contracts/openapi/paths/training.yaml"
    verification: "OpenAPI lint and contract-gate validation"
    evidence_refs:
      - "../../../../../_reports/contract_gates/latest.json"

  - id: TRAIN-TO-003
    scope: domain_invariants_and_permissions
    artifact_ref: "../../../../../src/training/tests/unit/test_invariants.py"
    covers:
      - INV-TRAIN-001
      - INV-TRAIN-002
      - INV-TRAIN-003
      - INV-TRAIN-004
      - INV-TRAIN-005
      - INV-TRAIN-006
      - INV-TRAIN-007
    verification: "Domain unit tests enforcing invariants, FSM, elastic-sum, wellness window, boundary rules and permission checks"
    evidence_refs:
      - "../../../../../src/training/tests/unit/test_invariants.py"

  - id: TRAIN-TO-004
    scope: source_graph_integrity
    artifact_ref: "../../../../../tests/pipeline_gates/test_training_source_graph_integrity.py"
    verification: "Pipeline gate enforces parity between graph, docs, contracts and runtime refs"
    evidence_refs:
      - "../../../../../tests/pipeline_gates/test_training_source_graph_integrity.py"
