# AR_002.5_B — Schema: attendance.presence_status + status 'justified'

**Status**: ✅ VERIFICADO
**Versão do Protocolo**: 1.1.0

## Descrição
LACUNA DETECTADA: PRD Seção 7 (US-001) especifica explicitamente 3 estados de presença: 'Present / Absent / Justified' ('Posso marcar Present/Absent/Justified com um clique'). O schema atual restringe presence_status a apenas 2 valores via CHECK constraint: ARRAY['present', 'absent']. O terceiro estado 'justified' é atualmente codificado como: presence_status='absent' + reason_absence IS NOT NULL. Esta ambiguidade causa dois problemas: (A) queries de relatório precisam de lógica condicional para distinguir 'faltou sem justificativa' vs. 'faltou com justificativa'; (B) o frontend precisa renderizar 3 botões mas o schema suporta apenas 2, gerando inconsistência de validação.

AÇÃO: Criar migration Alembic para ampliar o CHECK constraint da coluna presence_status na tabela attendance. Usar Python (.py).

MUDANÇA NO SCHEMA:
```sql
-- Remover constraint existente:
ALTER TABLE attendance DROP CONSTRAINT ck_attendance_status;
-- Adicionar nova com 3 valores:
ALTER TABLE attendance ADD CONSTRAINT ck_attendance_status
    CHECK (presence_status IN ('present', 'absent', 'justified'));
```

MIGRAÇÃO DE DADOS EXISTENTES:
```sql
-- Promover registros ausentes com reason_absence para 'justified':
UPDATE attendance
SET presence_status = 'justified'
WHERE presence_status = 'absent' AND reason_absence IS NOT NULL AND deleted_at IS NULL;
-- Nota: reason_absence permanece preenchido em 'justified' para detalhe.
```

REGRA DE NEGÓCIO RESULTANTE:
- 'present': atleta compareceu
- 'absent': faltou sem justificativa (reason_absence MUST be NULL)
- 'justified': faltou com motivo formal (reason_absence SHOULD be filled)
Adicionar CHECK para reforçar: CHECK ((presence_status != 'absent') OR (reason_absence IS NULL)) se a regra for formalizada.

NOME DA MIGRATION: 0056_attendance_add_justified_status.py

## Critérios de Aceite
1) Migration executada sem erro, inclusive o UPDATE de dados existentes. 2) INSERT INTO attendance (..., presence_status='justified') executado com sucesso (não viola constraint). 3) INSERT com presence_status='maybe' levanta constraint violation. 4) SELECT COUNT(*) FROM attendance WHERE presence_status='justified' retorna >= 0 (não erro). 5) docs/ssot/schema.sql atualizado.

## SSOT Touches
- [x] docs/ssot/schema.sql
- [x] docs/ssot/alembic_state.txt

## Validation Command (Contrato)
```
python temp/validate_ar002_5_b.py
```

## Evidence File (Contrato)
`docs/hbtrack/evidence/AR_002.5_B/executor_main.log`

## Rollback Plan (Contrato)
```bash
alembic downgrade -1
```
⚠️ **ATENÇÃO**: Este AR modifica banco. Execute rollback em caso de falha.

## Notas do Arquiteto
_(AR materializada pelo Executor para resolver órfão detectado pelo gate PLANS_AR_SYNC_CHECK. Arquiteto deve preencher detalhes de implementação.)_

## Riscos
- UPDATE de dados existentes pode afetar registros com deleted_at IS NOT NULL (filtrado no UPDATE).

## Análise de Impacto
- **Escopo (atômico):** criar uma migration Alembic única para evoluir o `CHECK` de `attendance.presence_status` e executar migração de dados legada para `justified`.
- **Numeração da migration:** como a cadeia atual já está em `0057`, será usada a próxima revisão livre `0058` com `down_revision='0057'`.
- **Mudança de integridade:** substituir `ck_attendance_status` para aceitar `('present','absent','justified')`.
- **Migração de dados:** promover registros ativos (`deleted_at IS NULL`) de `absent` com `reason_absence` preenchido para `justified`.
- **Regra de negócio formalizada:** adicionar `CHECK` adicional para impedir `absent` com justificativa em registros ativos (permitindo registros soft-deletados).
- **Riscos principais:** possível conflito se houver migration concorrente usando `0058`; mitigação por encadeamento direto em `0057` e execução determinística de uma AR por vez.
- **Rollback:** `alembic downgrade -1`, revertendo constraint de status para 2 valores e convertendo `justified` de volta para `absent`.

---
## Carimbo de Execução
_(Gerado por hb report)_

### Execução Executor em 15ac28c
**Status Executor**: 🏗️ EM_EXECUCAO
**Comando**: `python temp/validate_ar002_5_b.py`
**Exit Code**: 0
**Timestamp UTC**: 2026-02-24T16:07:37.184258+00:00
**Behavior Hash**: 8853f6a300ab0bc04892c733a361513dca571bd2b56f09aa4d1eb268b4808bba
**Evidence File**: `docs/hbtrack/evidence/AR_002.5_B/executor_main.log`
**Python Version**: 3.11.9


### Verificacao Testador em 15ac28c
**Status Testador**: ✅ SUCESSO
**Consistency**: OK
**Triple-Run**: OK (3x)
**Exit Testador**: 0 | **Exit Executor**: 0
**TESTADOR_REPORT**: `_reports/testador/AR_002.5_B_15ac28c/result.json`

### Selo Humano em c9f6f40
**Status Humano**: ✅ VERIFICADO
**Timestamp UTC**: 2026-02-24T16:20:54.736260+00:00
**Motivo**: —
**TESTADOR_REPORT**: `_reports/testador/AR_002.5_B_15ac28c/result.json`
**Evidence File**: `docs/hbtrack/evidence/AR_002.5_B/executor_main.log`
