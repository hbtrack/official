# AR_189 — Ciclos hierarchy: FK obrigatório + containment validation service

**Status**: ✅ VERIFICADO
**Versão do Protocolo**: 1.3.0

## Descrição
Implementar em dois arquivos: (1) Hb Track - Backend/app/models/training_cycle.py — Garantir que o modelo SQLAlchemy declare parent_cycle_id como FK obrigatório/nullable correto para meso (meso DEVE ter parent_cycle_id apontando para macro; macro DEVE ter parent_cycle_id=NULL). Verificar se há enum/check para type in ('macro','meso'). Inspecionar também training_microcycles model para garantir que cycle_id é FK NOT NULL para training_cycles.id. (2) Hb Track - Backend/app/services/training_cycle_service.py — Adicionar ou completar: (a) validação INV-054: ao criar microciclo, verificar que o mesocycle_id referenciado existe e é do tipo 'meso'; rejeitar com 422 se ausente/inválido [AC-001]; (b) validação INV-056: ao criar microciclo, verificar que week_start >= meso.start_date AND week_end <= meso.end_date; rejeitar com 422 se fora do range [AC-002]; (c) meso overlap (INV-055): ao criar mesociclo, NÃO rejeitar overlap com outro meso do mesmo macro — overlaps são PERMITIDOS [AC-003]. PROIBIDO: não criar nova tabela; não alterar schema se FK já existe no DB; não modificar routers. Se descobrir que alguma FK está faltando no DB (não só no model), criar migration Alembic e atualizar ssot via gen_docs_ssot.py.

## Critérios de Aceite
1) training_cycle_service.py rejeita criação de microciclo sem mesocycle_id válido (mock test ou assert no código). 2) training_cycle_service.py rejeita microciclo com datas fora do range do meso pai. 3) training_cycle_service.py NÃO rejeita dois mesos com overlap no mesmo macro. 4) INV-054: FK obrigatório declarado no model Python (nullable=False para microciclo.cycle_id). 5) INV-055: sem restrição de overlap entre mesos. 6) INV-056: containment de datas validado.

## Write Scope
- Hb Track - Backend/app/models/training_cycle.py
- Hb Track - Backend/app/services/training_cycle_service.py

## Validation Command (Contrato)
```
python -c "import os; b='Hb Track - Backend'; svc=open(os.path.join(b,'app','services','training_cycle_service.py')).read(); assert 'mesocycle_id' in svc or 'cycle_id' in svc, 'service missing cycle FK reference'; assert 'start_date' in svc or 'week_start' in svc, 'service missing date containment check'; assert 'containment' in svc.lower() or 'fora' in svc.lower() or 'range' in svc.lower() or 'within' in svc.lower() or 'week_start' in svc, 'service missing containment validation'; mdl=open(os.path.join(b,'app','models','training_cycle.py')).read(); assert 'parent_cycle_id' in mdl, 'model missing parent_cycle_id'; print('PASS AR_189')"
```

## Evidence File (Contrato)
`docs/hbtrack/evidence/AR_189/executor_main.log`

## Rollback Plan (Contrato)
```
git checkout -- Hb Track - Backend/app/models/training_cycle.py
git checkout -- Hb Track - Backend/app/services/training_cycle_service.py
```
⚠️ **ATENÇÃO**: Este AR modifica banco. Execute rollback em caso de falha.

## Notas do Arquiteto
Se Executor detectar que constraint de FK falta no DB (não só no model), deve criar migration Alembic e rodar gen_docs_ssot.py para atualizar schema.sql + alembic_state.txt. Nesse caso adicionar ssot_touches ao evidence. INV-055 é positivo: overlap de mesos é PERMITIDO, não é erro — garantir que o service não adicione restrição desnecessária de overlap.

## Riscos
- training_cycle_service.py pode já ter validação parcial — Executor ler antes de adicionar para evitar duplicação
- Tabela training_microcycles é separada de training_cycles — garantir que o service cria microciclo na tabela correta
- INV-055: overlap de mesos DEVE ser aceito — não inserir guard de overlap

## Análise de Impacto

**Inspeção pré-implementação (2026-03-01)**:
- `app/models/training_cycle.py`: `parent_cycle_id` já declarado como `Mapped[Optional[UUID]]` com FK `training_cycles_parent_cycle_id_fkey` → **modelo OK, sem alteração necessária**.
- `app/models/training_microcycle.py`: `cycle_id: Mapped[Optional[UUID]]` com FK para `training_cycles.id`. Campo é `nullable=True` no model — INV-054 diz que microciclo DEVE ter cycle_id; será enforçado no service (não alterar model autogenerated).
- `app/services/training_cycle_service.py`: Já tem `MicrocycleOutsideMesoError` e validação parcial de datas meso→macro. NÃO tem `validate_microcycle_containment` nem `mesocycle_id` como parâmetro. Falta: (1) método dedicado `validate_microcycle_containment(mesocycle_id, week_start, week_end)` para uso por `TrainingMicrocycleService`; (2) guard que EXIGE `parent_cycle_id` para tipo 'meso' (atual só valida se fornecido).
- Schema DB já tem FKs corretos — nenhuma migration necessária.

**Mudanças planejadas**:
1. `training_cycle_service.py` create(): adicionar guard `if data.type == 'meso' and not data.parent_cycle_id: raise ValidationError(...)`.
2. `training_cycle_service.py`: adicionar método `validate_microcycle_containment(mesocycle_id, week_start, week_end)` com INV-054 + INV-056.
3. Sem alteração no model (autogenerated) nem migration.

---
## Carimbo de Execução
_(Gerado por hb report)_

### Execução Executor em b123a58
**Status Executor**: 🏗️ EM_EXECUCAO
**Comando**: `python -c "import os; b='Hb Track - Backend'; svc=open(os.path.join(b,'app','services','training_cycle_service.py')).read(); assert 'mesocycle_id' in svc or 'cycle_id' in svc, 'service missing cycle FK reference'; assert 'start_date' in svc or 'week_start' in svc, 'service missing date containment check'; assert 'containment' in svc.lower() or 'fora' in svc.lower() or 'range' in svc.lower() or 'within' in svc.lower() or 'week_start' in svc, 'service missing containment validation'; mdl=open(os.path.join(b,'app','models','training_cycle.py')).read(); assert 'parent_cycle_id' in mdl, 'model missing parent_cycle_id'; print('PASS AR_189')"`
**Exit Code**: 0
**Timestamp UTC**: 2026-03-01T08:39:18.125579+00:00
**Behavior Hash**: d33b5eeeb589b2ce25e262f3d8906379b02585e896a8d397ac7fdaea7e4c36a0
**Evidence File**: `docs/hbtrack/evidence/AR_189/executor_main.log`
**Python Version**: 3.11.9


### Verificacao Testador em b123a58
**Status Testador**: ✅ SUCESSO
**Consistency**: OK
**Triple-Run**: OK (3x)
**Exit Testador**: 0 | **Exit Executor**: 0
**TESTADOR_REPORT**: `_reports/testador/AR_189_b123a58/result.json`

### Selo Humano em b123a58
**Status Humano**: ✅ VERIFICADO
**Timestamp UTC**: 2026-03-01T09:01:59.496738+00:00
**Motivo**: —
**TESTADOR_REPORT**: `_reports/testador/AR_189_b123a58/result.json`
**Evidence File**: `docs/hbtrack/evidence/AR_189/executor_main.log`
