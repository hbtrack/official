# Issues pré-existentes fora do escopo de E4.
# Workflows tocados por outras fases têm esses problemas herdados.
ignore:
  - '"github.head_ref" is potentially untrusted'
  - 'property "build" is not defined'
