# scripts/migrate

Data evolution (DB writes).

- **SHOULD** have rollback notes
- **MUST NOT** auto-run in CI unless explicitly controlled

See `scripts/_policy/templates/migrate/README.md` for template reference.
