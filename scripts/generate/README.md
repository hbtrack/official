# scripts/generate

Generate deterministic derived artifacts.

- **MUST** be idempotent (same inputs → same outputs)
- **MUST NOT** fix drift (that's fixes/)

See `scripts/_policy/templates/generate/README.md` for template reference.
