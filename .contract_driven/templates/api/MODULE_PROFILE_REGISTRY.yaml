version: "1.0.0"

modules:
  users:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  seasons:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  teams:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  training:
    module_class: "HYBRID"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  wellness:
    module_class: "HYBRID"
    enabled_surfaces: ["sync"]
    overlays: ["sensitive_overlay"]
    contract_targets:
      sync: "openapi"

  medical:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: ["sensitive_overlay"]
    contract_targets:
      sync: "openapi"

  competitions:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  matches:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  scout:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  exercises:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  analytics:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  reports:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  ai_ingestion:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: ["sensitive_overlay"]
    contract_targets:
      sync: "openapi"

  identity_access:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: ["sensitive_overlay"]
    contract_targets:
      sync: "openapi"

  audit:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  notifications:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

