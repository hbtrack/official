version: "1.0.0"

modules:
  users:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  seasons:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  teams:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  training:
    module_class: "HYBRID"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  wellness:
    module_class: "HYBRID"
    enabled_surfaces: ["sync", "event"]
    overlays: ["sensitive_overlay"]
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  medical:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: ["sensitive_overlay"]
    contract_targets:
      sync: "openapi"

  competitions:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  matches:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  scout:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  exercises:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  analytics:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  reports:
    module_class: "CRUD"
    enabled_surfaces: ["sync"]
    overlays: []
    contract_targets:
      sync: "openapi"

  ai_ingestion:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: ["sensitive_overlay"]
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  identity_access:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: ["sensitive_overlay"]
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  audit:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  notifications:
    module_class: "CRUD"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"

  video:
    module_class: "HYBRID"
    enabled_surfaces: ["sync", "event"]
    overlays: []
    contract_targets:
      sync: "openapi"
      event: "asyncapi"
