# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: notifications
# source_fingerprint: ff1df0bf0fefa6a65aa4392c2f712f7c85074a3598ec3870f141324eb64e6a0a

from __future__ import annotations

from datetime import datetime
from typing import Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import NotificationDeliveryModel
from ..domain.entities import NotificationDelivery


def _notification_delivery_from_model(model: NotificationDeliveryModel) -> NotificationDelivery:
    return NotificationDelivery(
        id=model.id,
        recipient_user_id=model.recipient_user_id,
        notification_template_ref=model.notification_template_ref,
        event_envelope_ref=model.event_envelope_ref,
        channel_label=model.channel_label,
        preference_label=model.preference_label,
        delivery_status_label=model.delivery_status_label,
        retry_count=model.retry_count,
        requested_at=model.requested_at,
        delivered_at=model.delivered_at,
    )


class NotificationDeliveryRepository:
    def save(self, notification_delivery: NotificationDelivery) -> NotificationDelivery:
        obj, _ = NotificationDeliveryModel.objects.update_or_create(
            id=notification_delivery.id,
            defaults={
            "recipient_user_id": notification_delivery.recipient_user_id,
            "notification_template_ref": notification_delivery.notification_template_ref,
            "event_envelope_ref": notification_delivery.event_envelope_ref,
            "channel_label": notification_delivery.channel_label,
            "preference_label": notification_delivery.preference_label,
            "delivery_status_label": notification_delivery.delivery_status_label,
            "retry_count": notification_delivery.retry_count,
            "requested_at": notification_delivery.requested_at,
            "delivered_at": notification_delivery.delivered_at,
            },
        )
        return _notification_delivery_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[NotificationDelivery]:
        try:
            return _notification_delivery_from_model(NotificationDeliveryModel.objects.get(id=entity_id))
        except NotificationDeliveryModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[NotificationDelivery], Optional[str]]:
        qs = NotificationDeliveryModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_notification_delivery_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
