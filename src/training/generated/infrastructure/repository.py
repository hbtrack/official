# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: training
# source_fingerprint: 8972fa763c21a1523af4b9b77f84333333a7b48005d6c91dadd03d2ab17b0223

from __future__ import annotations

from datetime import datetime
from decimal import Decimal
from typing import Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import TrainingSessionModel
from ..domain.entities import TrainingSession


def _training_session_from_model(model: TrainingSessionModel) -> TrainingSession:
    return TrainingSession(
        id=model.id,
        organization_id=model.organization_id,
        team_id=model.team_id,
        season_id=model.season_id,
        microcycle_id=model.microcycle_id,
        session_at=model.session_at,
        duration_planned_minutes=model.duration_planned_minutes,
        location=model.location,
        session_type=model.session_type,
        main_objective=model.main_objective,
        secondary_objective=model.secondary_objective,
        planned_load=model.planned_load,
        group_climate=model.group_climate,
        notes=model.notes,
        phase_focus_defense=model.phase_focus_defense,
        phase_focus_attack=model.phase_focus_attack,
        phase_focus_transition_offense=model.phase_focus_transition_offense,
        phase_focus_transition_defense=model.phase_focus_transition_defense,
        intensity_target=model.intensity_target,
        session_block=model.session_block,
        created_at=model.created_at,
        created_by_user_id=model.created_by_user_id,
        updated_at=model.updated_at,
        deleted_at=model.deleted_at,
        deleted_reason=model.deleted_reason,
        focus_attack_positional_pct=model.focus_attack_positional_pct,
        focus_defense_positional_pct=model.focus_defense_positional_pct,
        focus_transition_offense_pct=model.focus_transition_offense_pct,
        focus_transition_defense_pct=model.focus_transition_defense_pct,
        focus_attack_technical_pct=model.focus_attack_technical_pct,
        focus_defense_technical_pct=model.focus_defense_technical_pct,
        focus_physical_pct=model.focus_physical_pct,
        status=model.status,
        closed_at=model.closed_at,
        closed_by_user_id=model.closed_by_user_id,
        deviation_justification=model.deviation_justification,
        planning_deviation_flag=model.planning_deviation_flag,
        standalone=model.standalone,
        started_at=model.started_at,
        ended_at=model.ended_at,
        duration_actual_minutes=model.duration_actual_minutes,
        execution_outcome=model.execution_outcome,
        delay_minutes=model.delay_minutes,
        cancellation_reason=model.cancellation_reason,
        post_review_completed_at=model.post_review_completed_at,
        post_review_completed_by_user_id=model.post_review_completed_by_user_id,
        post_review_deadline_at=model.post_review_deadline_at,
        individualization_mode=model.individualization_mode,
        planned_content_snapshot=model.planned_content_snapshot,
        objective_origin=model.objective_origin,
        actual_load_recorded=model.actual_load_recorded,
        continuity_notes=model.continuity_notes,
    )


class TrainingSessionRepository:
    def save(self, training_session: TrainingSession) -> TrainingSession:
        obj, _ = TrainingSessionModel.objects.update_or_create(
            id=training_session.id,
            defaults={
            "organization_id": training_session.organization_id,
            "team_id": training_session.team_id,
            "season_id": training_session.season_id,
            "microcycle_id": training_session.microcycle_id,
            "session_at": training_session.session_at,
            "duration_planned_minutes": training_session.duration_planned_minutes,
            "location": training_session.location,
            "session_type": training_session.session_type,
            "main_objective": training_session.main_objective,
            "secondary_objective": training_session.secondary_objective,
            "planned_load": training_session.planned_load,
            "group_climate": training_session.group_climate,
            "notes": training_session.notes,
            "phase_focus_defense": training_session.phase_focus_defense,
            "phase_focus_attack": training_session.phase_focus_attack,
            "phase_focus_transition_offense": training_session.phase_focus_transition_offense,
            "phase_focus_transition_defense": training_session.phase_focus_transition_defense,
            "intensity_target": training_session.intensity_target,
            "session_block": training_session.session_block,
            "created_at": training_session.created_at,
            "created_by_user_id": training_session.created_by_user_id,
            "updated_at": training_session.updated_at,
            "deleted_at": training_session.deleted_at,
            "deleted_reason": training_session.deleted_reason,
            "focus_attack_positional_pct": training_session.focus_attack_positional_pct,
            "focus_defense_positional_pct": training_session.focus_defense_positional_pct,
            "focus_transition_offense_pct": training_session.focus_transition_offense_pct,
            "focus_transition_defense_pct": training_session.focus_transition_defense_pct,
            "focus_attack_technical_pct": training_session.focus_attack_technical_pct,
            "focus_defense_technical_pct": training_session.focus_defense_technical_pct,
            "focus_physical_pct": training_session.focus_physical_pct,
            "status": training_session.status,
            "closed_at": training_session.closed_at,
            "closed_by_user_id": training_session.closed_by_user_id,
            "deviation_justification": training_session.deviation_justification,
            "planning_deviation_flag": training_session.planning_deviation_flag,
            "standalone": training_session.standalone,
            "started_at": training_session.started_at,
            "ended_at": training_session.ended_at,
            "duration_actual_minutes": training_session.duration_actual_minutes,
            "execution_outcome": training_session.execution_outcome,
            "delay_minutes": training_session.delay_minutes,
            "cancellation_reason": training_session.cancellation_reason,
            "post_review_completed_at": training_session.post_review_completed_at,
            "post_review_completed_by_user_id": training_session.post_review_completed_by_user_id,
            "post_review_deadline_at": training_session.post_review_deadline_at,
            "individualization_mode": training_session.individualization_mode,
            "planned_content_snapshot": training_session.planned_content_snapshot,
            "objective_origin": training_session.objective_origin,
            "actual_load_recorded": training_session.actual_load_recorded,
            "continuity_notes": training_session.continuity_notes,
            },
        )
        return _training_session_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[TrainingSession]:
        try:
            return _training_session_from_model(TrainingSessionModel.objects.get(id=entity_id))
        except TrainingSessionModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[TrainingSession], Optional[str]]:
        qs = TrainingSessionModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_training_session_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
