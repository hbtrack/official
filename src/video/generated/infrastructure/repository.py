# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: video
# source_fingerprint: 5e6128dc84578428e8cd577bfae07817c1d05f7a4179786617f299ef68b0a04a

from __future__ import annotations

from datetime import datetime
from typing import Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import MatchMediaSessionModel
from ..domain.entities import MatchMediaSession


def _match_media_session_from_model(model: MatchMediaSessionModel) -> MatchMediaSession:
    return MatchMediaSession(
        id=model.id,
        match_id=model.match_id,
        state=model.state,
        capture_mode=model.capture_mode,
        retention_policy=model.retention_policy,
        last_timecode=model.last_timecode,
        technical_contact_user_id=model.technical_contact_user_id,
        created_at=model.created_at,
        created_by_user_id=model.created_by_user_id,
        capture_started_at=model.capture_started_at,
        sync_completed_at=model.sync_completed_at,
        transcode_completed_at=model.transcode_completed_at,
        published_at=model.published_at,
        segment_count=model.segment_count,
        total_duration_ms=model.total_duration_ms,
        active_transcode_job_count=model.active_transcode_job_count,
    )


class MatchMediaSessionRepository:
    def save(self, match_media_session: MatchMediaSession) -> MatchMediaSession:
        obj, _ = MatchMediaSessionModel.objects.update_or_create(
            id=match_media_session.id,
            defaults={
            "match_id": match_media_session.match_id,
            "state": match_media_session.state,
            "capture_mode": match_media_session.capture_mode,
            "retention_policy": match_media_session.retention_policy,
            "last_timecode": match_media_session.last_timecode,
            "technical_contact_user_id": match_media_session.technical_contact_user_id,
            "created_at": match_media_session.created_at,
            "created_by_user_id": match_media_session.created_by_user_id,
            "capture_started_at": match_media_session.capture_started_at,
            "sync_completed_at": match_media_session.sync_completed_at,
            "transcode_completed_at": match_media_session.transcode_completed_at,
            "published_at": match_media_session.published_at,
            "segment_count": match_media_session.segment_count,
            "total_duration_ms": match_media_session.total_duration_ms,
            "active_transcode_job_count": match_media_session.active_transcode_job_count,
            },
        )
        return _match_media_session_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[MatchMediaSession]:
        try:
            return _match_media_session_from_model(MatchMediaSessionModel.objects.get(id=entity_id))
        except MatchMediaSessionModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[MatchMediaSession], Optional[str]]:
        qs = MatchMediaSessionModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_match_media_session_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
