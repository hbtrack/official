# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: wellness
# source_fingerprint: a24f3c120e4d8b8dc45cb4f9ba73487a28d49139390d01546769d9bed8ad2676

from __future__ import annotations

from datetime import date
from typing import Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import WellnessEntryModel
from ..domain.entities import WellnessEntry


def _wellness_entry_from_model(model: WellnessEntryModel) -> WellnessEntry:
    return WellnessEntry(
        id=model.id,
        athlete_user_id=model.athlete_user_id,
        training_session_id=model.training_session_id,
        questionnaire_date=model.questionnaire_date,
        questionnaire_label=model.questionnaire_label,
        readiness_score=model.readiness_score,
        fatigue_score=model.fatigue_score,
        pain_score=model.pain_score,
        recovery_score=model.recovery_score,
        sleep_hours=model.sleep_hours,
        notes=model.notes,
    )


class WellnessEntryRepository:
    def save(self, wellness_entry: WellnessEntry) -> WellnessEntry:
        obj, _ = WellnessEntryModel.objects.update_or_create(
            id=wellness_entry.id,
            defaults={
            "athlete_user_id": wellness_entry.athlete_user_id,
            "training_session_id": wellness_entry.training_session_id,
            "questionnaire_date": wellness_entry.questionnaire_date,
            "questionnaire_label": wellness_entry.questionnaire_label,
            "readiness_score": wellness_entry.readiness_score,
            "fatigue_score": wellness_entry.fatigue_score,
            "pain_score": wellness_entry.pain_score,
            "recovery_score": wellness_entry.recovery_score,
            "sleep_hours": wellness_entry.sleep_hours,
            "notes": wellness_entry.notes,
            },
        )
        return _wellness_entry_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[WellnessEntry]:
        try:
            return _wellness_entry_from_model(WellnessEntryModel.objects.get(id=entity_id))
        except WellnessEntryModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[WellnessEntry], Optional[str]]:
        qs = WellnessEntryModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_wellness_entry_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
