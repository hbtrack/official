# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: exercises
# source_fingerprint: 67299d9912719912aa2df7c309c30c7cf414bc8deb71c0e6cad23e768edd29c4

from __future__ import annotations

from datetime import datetime
from typing import Any, Dict, List, Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import ExerciseModel
from ..domain.entities import Exercise


def _exercise_from_model(model: ExerciseModel) -> Exercise:
    return Exercise(
        id=model.id,
        organization_id=model.organization_id,
        scope=model.scope,
        visibility_mode=model.visibility_mode,
        name=model.name,
        description=model.description,
        session_phase=model.session_phase,
        primary_objective=model.primary_objective,
        secondary_objective=model.secondary_objective,
        game_phases=model.game_phases or [],
        age_categories=model.age_categories or [],
        skill_level=model.skill_level,
        complexity=model.complexity,
        physical_load=model.physical_load,
        min_athletes=model.min_athletes,
        max_athletes=model.max_athletes,
        estimated_duration_minutes=model.estimated_duration_minutes,
        space_required=model.space_required,
        materials=model.materials or [],
        instructions=model.instructions,
        coaching_cues=model.coaching_cues,
        safety_notes=model.safety_notes,
        thumbnail_url=model.thumbnail_url,
        editorial_status=model.editorial_status,
        current_version_id=model.current_version_id,
        current_version_number=model.current_version_number,
        created_by_user_id=model.created_by_user_id,
        created_at=model.created_at,
        updated_at=model.updated_at,
        deleted_at=model.deleted_at,
        deletion_reason=model.deletion_reason,
        current_version=model.current_version or {},
        is_deleted=model.is_deleted,
    )


class ExerciseRepository:
    def save(self, exercise: Exercise) -> Exercise:
        obj, _ = ExerciseModel.objects.update_or_create(
            id=exercise.id,
            defaults={
            "organization_id": exercise.organization_id,
            "scope": exercise.scope,
            "visibility_mode": exercise.visibility_mode,
            "name": exercise.name,
            "description": exercise.description,
            "session_phase": exercise.session_phase,
            "primary_objective": exercise.primary_objective,
            "secondary_objective": exercise.secondary_objective,
            "game_phases": exercise.game_phases,
            "age_categories": exercise.age_categories,
            "skill_level": exercise.skill_level,
            "complexity": exercise.complexity,
            "physical_load": exercise.physical_load,
            "min_athletes": exercise.min_athletes,
            "max_athletes": exercise.max_athletes,
            "estimated_duration_minutes": exercise.estimated_duration_minutes,
            "space_required": exercise.space_required,
            "materials": exercise.materials,
            "instructions": exercise.instructions,
            "coaching_cues": exercise.coaching_cues,
            "safety_notes": exercise.safety_notes,
            "thumbnail_url": exercise.thumbnail_url,
            "editorial_status": exercise.editorial_status,
            "current_version_id": exercise.current_version_id,
            "current_version_number": exercise.current_version_number,
            "created_by_user_id": exercise.created_by_user_id,
            "created_at": exercise.created_at,
            "updated_at": exercise.updated_at,
            "deleted_at": exercise.deleted_at,
            "deletion_reason": exercise.deletion_reason,
            "current_version": exercise.current_version,
            "is_deleted": exercise.is_deleted,
            },
        )
        return _exercise_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[Exercise]:
        try:
            return _exercise_from_model(ExerciseModel.objects.get(id=entity_id))
        except ExerciseModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[Exercise], Optional[str]]:
        qs = ExerciseModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_exercise_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
