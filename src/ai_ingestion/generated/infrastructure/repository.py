# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: ai_ingestion
# source_fingerprint: e64c0ea5c190a6d5ddead900c5d3513b27a8578c4c92c2a377c47a30e9769d6e

from __future__ import annotations

from datetime import datetime
from typing import Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import IngestionJobModel
from ..domain.entities import IngestionJob


def _ingestion_job_from_model(model: IngestionJobModel) -> IngestionJob:
    return IngestionJob(
        id=model.id,
        source_label=model.source_label,
        ingestion_mode=model.ingestion_mode,
        payload_schema_ref=model.payload_schema_ref,
        mapping_profile=model.mapping_profile,
        idempotency_key=model.idempotency_key,
        execution_binding_label=model.execution_binding_label,
        received_at=model.received_at,
        completed_at=model.completed_at,
        status_label=model.status_label,
        error_message=model.error_message,
        origin_job_id=model.origin_job_id,
    )


class IngestionJobRepository:
    def save(self, ingestion_job: IngestionJob) -> IngestionJob:
        obj, _ = IngestionJobModel.objects.update_or_create(
            id=ingestion_job.id,
            defaults={
            "source_label": ingestion_job.source_label,
            "ingestion_mode": ingestion_job.ingestion_mode,
            "payload_schema_ref": ingestion_job.payload_schema_ref,
            "mapping_profile": ingestion_job.mapping_profile,
            "idempotency_key": ingestion_job.idempotency_key,
            "execution_binding_label": ingestion_job.execution_binding_label,
            "received_at": ingestion_job.received_at,
            "completed_at": ingestion_job.completed_at,
            "status_label": ingestion_job.status_label,
            "error_message": ingestion_job.error_message,
            "origin_job_id": ingestion_job.origin_job_id,
            },
        )
        return _ingestion_job_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[IngestionJob]:
        try:
            return _ingestion_job_from_model(IngestionJobModel.objects.get(id=entity_id))
        except IngestionJobModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[IngestionJob], Optional[str]]:
        qs = IngestionJobModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_ingestion_job_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
