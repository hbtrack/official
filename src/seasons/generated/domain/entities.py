# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: seasons
# source_fingerprint: c8531bbe3968da55de9e401ff654c4c4aaa50b22fdefb931037cc3258ac3ce2a

from __future__ import annotations

from datetime import date, datetime
from typing import List, Optional
from uuid import UUID
from dataclasses import dataclass


VALID_STATUS_LABELS = frozenset(["DRAFT", "ACTIVE", "ARCHIVED"])


@dataclass
class Season:
    id: UUID
    name: str
    status_label: str
    start_date: date
    end_date: date
    phase_labels: List[str]
    competition_ids: List[UUID]
    team_ids: List[UUID]
    organization_id: Optional[UUID] = None
    sport_cycle_label: Optional[str] = None
    created_at: Optional[datetime] = None
    updated_at: Optional[datetime] = None

    def validate_invariants(self) -> None:
        """Validate domain invariants from entity graph."""
        if not self.id:
            raise ValueError("Season: id is required")
        if not self.name:
            raise ValueError("Season: name is required")
        if not self.status_label:
            raise ValueError("Season: status_label is required")
        if not self.start_date:
            raise ValueError("Season: start_date is required")
        if not self.end_date:
            raise ValueError("Season: end_date is required")
        if not self.phase_labels:
            raise ValueError("Season: phase_labels is required")
        if not self.competition_ids:
            raise ValueError("Season: competition_ids is required")
        if not self.team_ids:
            raise ValueError("Season: team_ids is required")
