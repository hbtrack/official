# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: seasons
# source_fingerprint: a3d6c3128a160e6b05d05b91f61bcc574b694116e3bf4843437987c5fb791b69

from __future__ import annotations

from datetime import date, datetime
from typing import List, Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import SeasonModel
from ..domain.entities import Season


def _season_from_model(model: SeasonModel) -> Season:
    return Season(
        id=model.id,
        organization_id=model.organization_id,
        name=model.name,
        sport_cycle_label=model.sport_cycle_label,
        status_label=model.status_label,
        start_date=model.start_date,
        end_date=model.end_date,
        phase_labels=model.phase_labels or [],
        competition_ids=model.competition_ids or [],
        team_ids=model.team_ids or [],
        created_at=model.created_at,
        updated_at=model.updated_at,
    )


class SeasonRepository:
    def save(self, season: Season) -> Season:
        obj, _ = SeasonModel.objects.update_or_create(
            id=season.id,
            defaults={
            "organization_id": season.organization_id,
            "name": season.name,
            "sport_cycle_label": season.sport_cycle_label,
            "status_label": season.status_label,
            "start_date": season.start_date,
            "end_date": season.end_date,
            "phase_labels": season.phase_labels,
            "competition_ids": season.competition_ids,
            "team_ids": season.team_ids,
            "created_at": season.created_at,
            "updated_at": season.updated_at,
            },
        )
        return _season_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[Season]:
        try:
            return _season_from_model(SeasonModel.objects.get(id=entity_id))
        except SeasonModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[Season], Optional[str]]:
        qs = SeasonModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_season_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
