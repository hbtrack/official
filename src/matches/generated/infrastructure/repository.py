# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: matches
# source_fingerprint: 4d848c9ada53bc78667f619018c4f41f1a0fb5c590e77645e02c509906c5df2f

from __future__ import annotations

from datetime import datetime
from typing import List, Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import MatchModel
from ..domain.entities import Match


def _match_from_model(model: MatchModel) -> Match:
    return Match(
        id=model.id,
        competition_id=model.competition_id,
        home_team_id=model.home_team_id,
        away_team_id=model.away_team_id,
        venue_label=model.venue_label,
        status_label=model.status_label,
        scheduled_at=model.scheduled_at,
        started_at=model.started_at,
        ended_at=model.ended_at,
        home_score=model.home_score,
        away_score=model.away_score,
        lineup_user_ids=model.lineup_user_ids or [],
        official_incident_ids=model.official_incident_ids or [],
        referee_names=model.referee_names or [],
        created_at=model.created_at,
        updated_at=model.updated_at,
    )


class MatchRepository:
    def save(self, match: Match) -> Match:
        obj, _ = MatchModel.objects.update_or_create(
            id=match.id,
            defaults={
            "competition_id": match.competition_id,
            "home_team_id": match.home_team_id,
            "away_team_id": match.away_team_id,
            "venue_label": match.venue_label,
            "status_label": match.status_label,
            "scheduled_at": match.scheduled_at,
            "started_at": match.started_at,
            "ended_at": match.ended_at,
            "home_score": match.home_score,
            "away_score": match.away_score,
            "lineup_user_ids": match.lineup_user_ids,
            "official_incident_ids": match.official_incident_ids,
            "referee_names": match.referee_names,
            "created_at": match.created_at,
            "updated_at": match.updated_at,
            },
        )
        return _match_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[Match]:
        try:
            return _match_from_model(MatchModel.objects.get(id=entity_id))
        except MatchModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[Match], Optional[str]]:
        qs = MatchModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_match_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
