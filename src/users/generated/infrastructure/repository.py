# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: users
# source_fingerprint: 67c24b3cd6d6764888f80334f9503421261becadce6b6fa96ae42adee2d8eb16

from __future__ import annotations

from typing import List, Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import UserProfileModel
from ..domain.entities import UserProfile


def _user_profile_from_model(model: UserProfileModel) -> UserProfile:
    return UserProfile(
        id=model.id,
        organization_id=model.organization_id,
        first_name=model.first_name,
        last_name=model.last_name,
        display_name=model.display_name,
        avatar_url=model.avatar_url,
        role_label=model.role_label,
        status_label=model.status_label,
        position_label=model.position_label,
        preferred_language=model.preferred_language,
        preference_tags=model.preference_tags or [],
        team_ids=model.team_ids or [],
        season_ids=model.season_ids or [],
    )


class UserProfileRepository:
    def save(self, user_profile: UserProfile) -> UserProfile:
        obj, _ = UserProfileModel.objects.update_or_create(
            id=user_profile.id,
            defaults={
            "organization_id": user_profile.organization_id,
            "first_name": user_profile.first_name,
            "last_name": user_profile.last_name,
            "display_name": user_profile.display_name,
            "avatar_url": user_profile.avatar_url,
            "role_label": user_profile.role_label,
            "status_label": user_profile.status_label,
            "position_label": user_profile.position_label,
            "preferred_language": user_profile.preferred_language,
            "preference_tags": user_profile.preference_tags,
            "team_ids": user_profile.team_ids,
            "season_ids": user_profile.season_ids,
            },
        )
        return _user_profile_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[UserProfile]:
        try:
            return _user_profile_from_model(UserProfileModel.objects.get(id=entity_id))
        except UserProfileModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[UserProfile], Optional[str]]:
        qs = UserProfileModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_user_profile_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
