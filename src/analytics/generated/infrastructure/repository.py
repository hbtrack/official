# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: analytics
# source_fingerprint: 9ab39092aa289fdce4caaddc393cd10ce981af2bdee441e3f36bd60a36143722

from __future__ import annotations

from datetime import datetime
from typing import List, Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import AnalyticsSnapshotModel
from ..domain.entities import AnalyticsSnapshot


def _analytics_snapshot_from_model(model: AnalyticsSnapshotModel) -> AnalyticsSnapshot:
    return AnalyticsSnapshot(
        id=model.id,
        metric_key=model.metric_key,
        source_module_labels=model.source_module_labels or [],
        time_window_label=model.time_window_label,
        granularity_label=model.granularity_label,
        filter_summary=model.filter_summary,
        projection_key=model.projection_key,
        refresh_mode_label=model.refresh_mode_label,
        computed_at=model.computed_at,
        created_by_user_id=model.created_by_user_id,
    )


class AnalyticsSnapshotRepository:
    def save(self, analytics_snapshot: AnalyticsSnapshot) -> AnalyticsSnapshot:
        obj, _ = AnalyticsSnapshotModel.objects.update_or_create(
            id=analytics_snapshot.id,
            defaults={
            "metric_key": analytics_snapshot.metric_key,
            "source_module_labels": analytics_snapshot.source_module_labels,
            "time_window_label": analytics_snapshot.time_window_label,
            "granularity_label": analytics_snapshot.granularity_label,
            "filter_summary": analytics_snapshot.filter_summary,
            "projection_key": analytics_snapshot.projection_key,
            "refresh_mode_label": analytics_snapshot.refresh_mode_label,
            "computed_at": analytics_snapshot.computed_at,
            "created_by_user_id": analytics_snapshot.created_by_user_id,
            },
        )
        return _analytics_snapshot_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[AnalyticsSnapshot]:
        try:
            return _analytics_snapshot_from_model(AnalyticsSnapshotModel.objects.get(id=entity_id))
        except AnalyticsSnapshotModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[AnalyticsSnapshot], Optional[str]]:
        qs = AnalyticsSnapshotModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_analytics_snapshot_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
