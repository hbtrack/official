# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: competitions
# source_fingerprint: 319936c3b5198e07fc0513c4a479abe9df7f729c035b8a118ddd7d7ab3c4cb99

from __future__ import annotations

from datetime import date, datetime
from typing import List, Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import CompetitionModel
from ..domain.entities import Competition


def _competition_from_model(model: CompetitionModel) -> Competition:
    return Competition(
        id=model.id,
        season_id=model.season_id,
        organization_id=model.organization_id,
        name=model.name,
        format_label=model.format_label,
        status_label=model.status_label,
        start_date=model.start_date,
        end_date=model.end_date,
        stage_labels=model.stage_labels or [],
        calendar_entry_ids=model.calendar_entry_ids or [],
        registration_team_ids=model.registration_team_ids or [],
        standings_summary=model.standings_summary,
        created_at=model.created_at,
        updated_at=model.updated_at,
    )


class CompetitionRepository:
    def save(self, competition: Competition) -> Competition:
        obj, _ = CompetitionModel.objects.update_or_create(
            id=competition.id,
            defaults={
            "season_id": competition.season_id,
            "organization_id": competition.organization_id,
            "name": competition.name,
            "format_label": competition.format_label,
            "status_label": competition.status_label,
            "start_date": competition.start_date,
            "end_date": competition.end_date,
            "stage_labels": competition.stage_labels,
            "calendar_entry_ids": competition.calendar_entry_ids,
            "registration_team_ids": competition.registration_team_ids,
            "standings_summary": competition.standings_summary,
            "created_at": competition.created_at,
            "updated_at": competition.updated_at,
            },
        )
        return _competition_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[Competition]:
        try:
            return _competition_from_model(CompetitionModel.objects.get(id=entity_id))
        except CompetitionModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[Competition], Optional[str]]:
        qs = CompetitionModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_competition_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
