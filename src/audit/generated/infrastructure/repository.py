# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: audit
# source_fingerprint: c7add13c5e36cdee0a3c68eabcefdcb0124e63829d0896644e2f70d529546055

from __future__ import annotations

from datetime import datetime
from typing import Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import AuditEntryModel
from ..domain.entities import AuditEntry


def _audit_entry_from_model(model: AuditEntryModel) -> AuditEntry:
    return AuditEntry(
        id=model.id,
        actor_user_id=model.actor_user_id,
        target_resource_id=model.target_resource_id,
        target_resource_type=model.target_resource_type,
        action=model.action,
        outcome_label=model.outcome_label,
        origin_label=model.origin_label,
        correlation_id=model.correlation_id,
        occurred_at=model.occurred_at,
        before_summary=model.before_summary,
        after_summary=model.after_summary,
    )


class AuditEntryRepository:
    def save(self, audit_entry: AuditEntry) -> AuditEntry:
        obj, _ = AuditEntryModel.objects.update_or_create(
            id=audit_entry.id,
            defaults={
            "actor_user_id": audit_entry.actor_user_id,
            "target_resource_id": audit_entry.target_resource_id,
            "target_resource_type": audit_entry.target_resource_type,
            "action": audit_entry.action,
            "outcome_label": audit_entry.outcome_label,
            "origin_label": audit_entry.origin_label,
            "correlation_id": audit_entry.correlation_id,
            "occurred_at": audit_entry.occurred_at,
            "before_summary": audit_entry.before_summary,
            "after_summary": audit_entry.after_summary,
            },
        )
        return _audit_entry_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[AuditEntry]:
        try:
            return _audit_entry_from_model(AuditEntryModel.objects.get(id=entity_id))
        except AuditEntryModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[AuditEntry], Optional[str]]:
        qs = AuditEntryModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_audit_entry_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
