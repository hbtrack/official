# Auto-generated by scripts/generate/backend_codegen.py.
# DO NOT EDIT MANUALLY.
# generator: hbtrack_backend_codegen@0.1.0
# module: medical
# source_fingerprint: 0ca441b290a1871df0f4daa3093237e5355853821432b3548b2f7133962b3448

from __future__ import annotations

from datetime import date, datetime
from typing import Optional
from uuid import UUID
from typing import List, Tuple

from ...infrastructure.models import MedicalRecordModel
from ..domain.entities import MedicalRecord


def _medical_record_from_model(model: MedicalRecordModel) -> MedicalRecord:
    return MedicalRecord(
        id=model.id,
        athlete_user_id=model.athlete_user_id,
        team_id=model.team_id,
        record_date=model.record_date,
        record_label=model.record_label,
        assessment_summary=model.assessment_summary,
        restriction_summary=model.restriction_summary,
        return_to_training_authorized=model.return_to_training_authorized,
        return_to_play_authorized=model.return_to_play_authorized,
        clinical_notes=model.clinical_notes,
        is_deleted=model.is_deleted,
        deleted_at=model.deleted_at,
        created_at=model.created_at,
        updated_at=model.updated_at,
    )


class MedicalRecordRepository:
    def save(self, medical_record: MedicalRecord) -> MedicalRecord:
        obj, _ = MedicalRecordModel.objects.update_or_create(
            id=medical_record.id,
            defaults={
            "athlete_user_id": medical_record.athlete_user_id,
            "team_id": medical_record.team_id,
            "record_date": medical_record.record_date,
            "record_label": medical_record.record_label,
            "assessment_summary": medical_record.assessment_summary,
            "restriction_summary": medical_record.restriction_summary,
            "return_to_training_authorized": medical_record.return_to_training_authorized,
            "return_to_play_authorized": medical_record.return_to_play_authorized,
            "clinical_notes": medical_record.clinical_notes,
            "is_deleted": medical_record.is_deleted,
            "deleted_at": medical_record.deleted_at,
            "created_at": medical_record.created_at,
            "updated_at": medical_record.updated_at,
            },
        )
        return _medical_record_from_model(obj)

    def get_by_id(self, entity_id: UUID) -> Optional[MedicalRecord]:
        try:
            return _medical_record_from_model(MedicalRecordModel.objects.get(id=entity_id))
        except MedicalRecordModel.DoesNotExist:
            return None

    def list_entities(
        self,
        page_size: int = 20,
        page_token: Optional[str] = None,
    ) -> Tuple[List[MedicalRecord], Optional[str]]:
        qs = MedicalRecordModel.objects.all()
        offset = 0
        if page_token:
            try:
                offset = int(page_token)
            except ValueError:
                offset = 0
        total = qs.count()
        items = qs[offset: offset + page_size]
        entities = [_medical_record_from_model(m) for m in items]
        next_token = str(offset + page_size) if (offset + page_size) < total else None
        return entities, next_token
