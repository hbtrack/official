# scripts/seeds

Populate baseline data.

- **official/** → minimal & reproducible
- **test/** → deterministic & CI-safe
- **dev/** → local only (gitignored)
- **_archived/** → deprecated

See `scripts/_policy/templates/seeds/README.md` for template reference.
