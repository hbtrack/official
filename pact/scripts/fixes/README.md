# scripts/fixes

Correct drift (writes allowed).

- **MUST** log effects to `scripts/artifacts/fixes/`
- **SHOULD** be idempotent
- Outputs must be explicit (no silent changes)

See `scripts/_policy/templates/fixes/README.md` for template reference.
